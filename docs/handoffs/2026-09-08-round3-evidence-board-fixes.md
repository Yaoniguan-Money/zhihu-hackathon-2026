# 2026-09-08 · Round 3：证据板拼图体验修复

- 状态：complete
- 负责人：AI 代理（持续迭代第 3 轮）

## 实际完成（`app/game/evidence/page.tsx`）

1. **芯片溢出泳道（根因修复）**：芯片宽度从 `w-[72%]`（板宽的 72%，必然横跨泳道）改为 `w-[15.5%] min-w-[150px]`（≈单个泳道宽），芯片不再跨道、不再被板缘裁切。
2. **默认放置排队**：addEvidence 由"伪随机纵坐标"改为泳道内竖向排队（`y = 0.16 + 泳道内数量×0.19`，上限 0.9），新芯片不再压住旧芯片。
3. **连线可发现性**：进入连线模式时板面顶部出现醒目横幅「连线中：再点目标证据的『连线』按钮完成关系，Esc 取消」；新增 Esc 取消连线模式。
4. **对质按钮静默无效**：目录自带 quote 证据没有 `source_message_id`，点了没反应；现在该情况直接不渲染对质按钮。

## 过程中的自纠

- Esc 取消的 useEffect 初版误放在组件早退 return 之后，触发 React Rules of Hooks 报错（开发浮层捕获），已移至 hooks 区并通过 typecheck 与实机重载验证。

## 验证

- `bun run typecheck` 通过。
- 实机：证据板页正常渲染、无 Hooks 报错；芯片新宽度的带量放置效果将在第 4 轮完整对局中验证（当前会话已揭底、板面为空，无法摆放）。

## 新发现（未修，进总账）

- **P1 证据板未保存改动静默丢失**：本地 placements 在路由切换时不持久化、无离开警告；第 0 轮实测摆放后切页即丢，rev 一直为 0。（第 4 轮修：sessionStorage 草稿或离开警告）
- P2：已揭底会话的证据板空池时提示"证据都已上板"与空板事实矛盾（文案状态机）。
- P2：连线第二步要求点目标块的「连线」按钮而非芯片本体——横幅已说明，但后续可改为点芯片本体直接完成（更直觉）。

## 下一位 Agent 最小阅读顺序

`app/game/evidence/page.tsx`（addEvidence、BoardChip className、连线横幅、onConfront 条件）。
