# 2026-09-08 · Round 8：小视口走查 + 文案打磨

- 状态：complete
- 负责人：AI 代理（持续迭代第 8 轮）

## 实际完成

1. **简报页角色卡名称截断**（`app/game/briefing/page.tsx`）：`truncate` → `line-clamp-2`，"沈青梧 · 财经调查记者"这类长名可完整换行显示，不再丢失身份信息（身份是玩家指认的重要线索）。
2. **证据池空态文案矛盾**（`app/game/evidence/page.tsx`）：原先 `pool` 为空一律显示"证据都已上板"，与空板事实矛盾；现按 `evidences.length` 区分"还没有解锁证据。先去审讯盘问角色吧。"与"证据都已上板。"两种状态。
3. **移动端视口（390×844）走查**：大厅面板 `max-w-[92vw]` 自适应、可滚动、四段动线与「我的案件」完整可读，无布局破裂。游戏以桌面端为主，移动端可用即可（记录为已知边界）。

## 验证

- `bun run typecheck` 通过；移动端与桌面端实机截图走查。

## 遗留

- 桌面 1440×900 为设计基准；平板中间尺寸未专项走查。
- 简报页角色卡换行后卡片高度增加，未发现挤压（实机确认）。

## 下一位 Agent 最小阅读顺序

`app/game/briefing/page.tsx` 角色卡；`app/game/evidence/page.tsx` 空态文案。
